# Copy this to .env.local and fill in your values for local development.
# API_BASE_URL is server-side only (no NEXT_PUBLIC_) — used by Next.js rewrites.
API_BASE_URL=http://localhost:8000

# Optional: Mapbox token for dark map tiles (if not set, falls back to maplibre free tiles)
# NEXT_PUBLIC_MAPBOX_TOKEN=your-mapbox-token-here
