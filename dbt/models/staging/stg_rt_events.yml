version: 2

models:
  - name: stg_rt_events
    columns:
      - name: feed_ts_utc
        data_tests:
          - not_null
      - name: route_id
        data_tests:
          - relationships:
              to: ref('stg_gtfs_routes')
              field: route_id
